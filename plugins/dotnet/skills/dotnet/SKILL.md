---
name: dotnet
description: >
  Use when working on .NET/C# backend code or tests — EF Core migrations (Postgres),
  Schema/Views/ModelSnapshot-Drift, or xUnit-Testkonventionen (AAA, Naming, Mocking).
  Dispatcher auf die einzelnen dotnet-* Skills. Triggers: @dotnet, @ef-migration,
  dotnet ef migrations add, Migrations-Ordner, xUnit, Testklasse.
---

# .NET

Passenden Skill unten per Skill-Tool aufrufen — nicht alle vorab laden.

| Thema | Skill |
|-------|-------|
| EF Core Migrations (Postgres, Views, ModelSnapshot-Drift) | dotnet-ef-migrations |
| xUnit-Testkonventionen (AAA, Naming, Mocking, WebApplicationFactory) | dotnet-xunit-conventions |
